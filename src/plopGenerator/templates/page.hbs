import React from 'react'
import { useSelector } from 'react-redux'
import { createStructuredSelector, createSelector } from 'reselect'
import Page from '@src/components/fragments/Page'


export default () => {

  return (
    <Page>
      <div>Add Your Content Here!</div>
      <div>在此添加内容!</div>
    </Page>
  )
}

const selectState = createStructuredSelector({
  tagDetailDefines: createSelector(
    selectZaTagCreationState,
    state => state.tagDetailDefines
  )
})
