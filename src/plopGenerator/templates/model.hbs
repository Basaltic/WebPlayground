import produce from 'immer';
import { createModel } from '@rematch/core'
import { defaultState } from './{{name}}State'

export const {{name}} = createModel({
  state: defaultState,
  reducers: {},
  effects: {}
})


